import java.util.*;

public class SortSetAndStoreInList {
    public List<String> sortSet(TreeSet<String> input)
    {
        /*Using Treeset to store words in lexiographic order
        and store result in arraylist*/
        List<String> output=new ArrayList<String>(input);
        return output;
    }
}
